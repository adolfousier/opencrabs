# HEARTBEAT.md

# Periodic checklist. Nothing fires on its own: this file is read on demand,
# or by a cron job (cron_manage) whose prompt tells the agent to read it.
# Keep it tiny — it is loaded into the turn that reads it. Example items:
# - inbox zero
# - calendar events in the next 2 hours
# - CI mentions needing review
